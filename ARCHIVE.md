# Archived: Python todoism

This branch freezes the **last Python implementation** of todoism
(as of the v1.21.9 line on `main` before the Rust rewrite).

- Install / run: see `README.md` (`pip install todoism`)
- Package: `todoism/`
- Do not merge this branch into the Rust `main`.

Rust development continues on `main`.
